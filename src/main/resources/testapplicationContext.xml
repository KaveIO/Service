<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
       xmlns:util="http://www.springframework.org/schema/util"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans 
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/util 
        http://www.springframework.org/schema/util/spring-util.xsd 
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">
    
    <context:component-scan base-package="nl.kpmg.af.service.service" />
        
    <context:property-placeholder location="classpath:mongotest.properties"  />
   
         
    <bean id="databaseInitialiser" class="nl.kpmg.af.service.request.filter.timestamp.DatabaseInitialiser"/>
     
    
    
     <!--<bean class="DatabaseInitialiser">
        <property name="dataSource" ref="dataSource"/>
    </bean>-->
    
    <bean id="mongoDBUtil" class="nl.kpmg.af.service.MongoDBUtil">
        <property name="url" value="${mongodb.url}"/>
        <!--<property name="host" value="${mongodb.host}"/>-->
        <property name="port" value="${mongodb.port}"/>
        <property name="username" value="${mongodb.username}"/>
        <property name="password" value="${mongodb.password}"/>
        <property name="database" value="${mongodb.database}"/>
    </bean>
</beans>